import React from "react";
import { Text } from "react-native";

const StopTuneButton = (props) => {
  return (
    <Text style={{ color: "#FFFFFF", fontFamily: "NovaSlim_400Regular" }}>
      Stop Tune
    </Text>
  );
};

export default StopTuneButton;
